# 予算・入出金管理システム 実施計画（2026年7月29日以降）

## 目的

既存の東京圏輸送情報システムのFirebase認証と管理者権限を引き継ぎ、年度別の予算、入出金、年会費、領収書、バックアップを一画面で管理できるようにする。

## 実装済みの初期版

- `budget_management.html`: 単一ページの管理画面
- `firestore.accounting.rules`: 既存Firestoreルールへ追加する管理者限定・型/範囲検証ルール例
- 既存の `users/{uid}` にある `status` と `isAdmin` を使用したアクセス制御
- 年度別の予算・入出金・年会費管理
- 検索、絞り込み、並び替え、カテゴリ別集計、進捗バー
- 年会費の支払済更新時に、固定IDの収入取引をトランザクションで同期（二重計上防止）
- 保存中のボタン無効化、入力値検証、削除時の確認コード入力
- JSONバックアップ、各種CSV、年度報告書と領収書の印刷/PDF保存
- 予算超過・予算未設定支出・未収会費の自動警告
- 月別収支集計と入出金の月別絞り込み
- 前年度予算のコピー（既存カテゴリは上書きしない）
- 未収メンバーの会費一括設定
- 予算重複・不正値・年会費連携漏れの整合性チェック

## 推奨スケジュール

### 7月29日〜7月31日: Firebase整理・検証環境

1. 現行Firestoreの `budgets`、`transactions`、`annual_fees` の残骸をエクスポートする。
2. 残骸は直ちに削除せず、内容と作成者を確認して移行対象を決める。
3. 既存ルールへ `firestore.accounting.rules` の内容を統合する。
4. 管理者アカウントと一般アカウントで、許可・拒否が正しいことをFirebase Emulatorまたは検証プロジェクトで確認する。

### 8月1日〜8月4日: データ移行・運用テスト

1. 年度、金額、日付、種別の不正データを修正する。
2. 予算カテゴリ名の表記揺れを整理する。
3. 年会費対象者をユーザー一覧から反映し、氏名・組織名・会費・免除対象を登録する。
4. 会費の支払済更新で収入が一度だけ作成されることを確認する。
5. 編集、削除、年度切替、CSV/JSON、帳票印刷を担当者2名以上で試す。

### 8月5日〜8月7日: 本番導入

1. 本番反映直前にFirestoreをバックアップする。
2. `budget_management.html` を公開リポジトリへ追加する。
3. `account_management.html` のメニューへ「会計管理」リンクを追加する。
4. Firestoreルールを反映し、管理者以外からアクセスできないことを再確認する。
5. 最初の1週間は毎日JSONバックアップを保存する。

## Firestoreコレクション

| コレクション | 主な用途 | ID方針 |
|---|---|---|
| `budgets` | 年度別予算 | 自動ID |
| `transactions` | 収入・支出 | 通常は自動ID。年会費は `annual_fee_{year}_{uid}` |
| `annual_fees` | メンバー別年会費 | `{year}_{uid}` |
| `users` | 既存ユーザー・管理者判定 | 既存UIDを継続 |

## 本番前の必須確認

- Firebaseコンソールのクライアント設定は公開情報だが、Firestoreルールが管理者限定になっていること。
- 一般ユーザーが3つの会計コレクションを直接読み書きできないこと。
- 年会費を「支払済→未収」「支払済→免除」へ戻した場合、対応する収入取引が削除されること。
- JSONバックアップを別の安全な場所へ保管できること。
- 領収書の発行者名、但し書き、連番の運用ルールを最終決定すること。

## 次段階で推奨する改善

- Cloud Functionsで領収書番号を厳密な連番にし、発行履歴を改ざんできない形で保存する。
- Firebase App Checkを有効化する。
- 変更履歴（監査ログ）コレクションを追加する。
- JSONバックアップの復元機能は、誤上書き防止のためプレビューと差分確認を備えた別画面として実装する。
