(function(){
  "use strict";
  const API="https://photo-api.tayunet-traininfo.com";
  const token=new URLSearchParams(location.search).get("token")||"";
  const content=document.getElementById("public-content"),unlock=document.getElementById("public-unlock");
  const esc=value=>String(value??"").replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"})[c]);
  let password="",objectUrls=[];
  async function request(path){const response=await fetch(`${API}${path}`,{headers:password?{"X-Share-Password":password}:{},cache:"no-store"});if(response.status===401){unlock.hidden=false;content.hidden=true;throw new Error("password")}if(!response.ok)throw new Error(response.status===404?"共有リンクが無効または期限切れです。":"共有記録を読み込めませんでした。");return response}
  async function media(photo,variant="thumbnail"){const response=await request(`/public/${encodeURIComponent(token)}/media/${photo.id}/${variant}`);const url=URL.createObjectURL(await response.blob());objectUrls.push(url);return url}
  function rows(photo){return [["列車番号",photo.trainNumber],["始発・終着",[photo.origin,photo.destination].filter(Boolean).join(" → ")],["列車種別",photo.trainType],["始発駅日付",photo.serviceDate],["変更事項",photo.changes],["撮影日時",photo.capturedAt],["撮影場所",photo.location],["駅",photo.station],["輸送経路",photo.transportRoute],["記事・備考",photo.article],["フリーメモ",photo.notes],["カメラ",photo.camera],["レンズ",photo.lens],["シャッター",photo.shutterSpeed],["F値",photo.aperture],["ISO",photo.iso],["焦点距離",photo.focalLength],["タグ",photo.tags.join(" / ")]].filter(([,v])=>v).map(([k,v])=>`<div class="data-group"><dt>${esc(k)}</dt><dd>${esc(v)}</dd></div>`).join("")}
  async function load(){try{const response=await request(`/public/${encodeURIComponent(token)}`),data=await response.json();unlock.hidden=true;content.hidden=false;if(data.type==="photo"){const photo=data.item;content.innerHTML=`<article class="public-record"><div class="detail-top"><h2>${esc(photo.trainNumber?`第 ${photo.trainNumber} 列車`:photo.title||photo.filename)}</h2></div><div class="detail-layout"><div class="detail-photo"><div class="spinner"></div></div><dl class="detail-data">${rows(photo)}</dl></div></article>`;document.querySelector(".detail-photo").innerHTML=`<img src="${await media(photo,"original")}" alt="${esc(photo.title||photo.filename)}">`}else{content.innerHTML=`<h1>${esc(data.album.title)}</h1><p>${esc(data.album.description)}</p><div class="public-album">${data.items.map(photo=>`<figure data-id="${photo.id}"><div class="photo-image loading-card"><img alt="${esc(photo.title||photo.filename)}"></div><figcaption><b>${esc(photo.title||photo.trainNumber||photo.filename)}</b><br><small>${esc(photo.capturedAt||photo.location||"")}</small></figcaption></figure>`).join("")}</div>`;await Promise.all(data.items.map(async photo=>{document.querySelector(`[data-id="${photo.id}"] img`).src=await media(photo)}))}}catch(error){if(error.message!=="password"){content.hidden=false;content.innerHTML=`<div class="unlock-card"><h1>表示できません</h1><p>${esc(error.message)}</p></div>`}}}
  document.getElementById("unlock-form").addEventListener("submit",event=>{event.preventDefault();password=document.getElementById("share-password").value;load()});window.addEventListener("beforeunload",()=>objectUrls.forEach(URL.revokeObjectURL));if(!token){content.innerHTML='<div class="unlock-card"><h1>共有リンクがありません</h1></div>'}else load();
})();
