# 運行情報ページ刷新版

## 反映するファイル

リポジトリ直下の次の3ファイルを同じ階層へ配置します。

- `train_grasp.html`
- `train_zai-data.html`
- `train_dashboard.css`（新規）

既存の `auth_guard.js`、Firebase SDK、`/api/odpt/challenge/` のプロキシをそのまま利用します。

## 画面の役割

### 運行把握 BOARD

- 路線単位でネットワーク全体を俯瞰
- 平常・遅延・大幅な乱れをラインボード上で色分け
- ODPTの運行情報と在線列車の遅延集計を併用
- 影響度ランキングで優先して確認すべき路線を判断

### 列車在線 DATA

- 列車番号、路線、行先、在線位置の横断検索
- 路線、方面、遅延状況の複合絞り込み
- PCでは一覧表、スマートフォンではカード表示
- 選択した列車の詳細を画面右側に表示

### ATOS Webとの棲み分け

- `train_grasp.html`: 全体俯瞰・異常の発見・影響度判断
- `train_zai-data.html`: 特定列車の検索・現在位置の確認
- `atosweb.html`: 時刻表・駅・列車の詳細調査

## 更新間隔

両ページとも60秒ごとに自動更新し、手動更新にも対応しています。
